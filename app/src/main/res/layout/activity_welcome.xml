<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F9FA"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp"
    tools:context=".activity_welcome">

    <!-- Logo Container - Simplified -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="48dp"
        android:background="@drawable/elementialogo"
        android:elevation="4dp" />

    <!-- Welcome Text -->
    <TextView
        android:id="@+id/welcomeTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:text="Welcome to Elementia!"
        android:textColor="#2C3E50"
        android:textSize="32sp"
        android:textStyle="bold" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:fontFamily="sans-serif-light"
        android:gravity="center"
        android:text="Explore the Periodic Table Like Never Before!"
        android:textColor="#7F8C8D"
        android:textSize="16sp" />

    <!-- Circular Continue Button -->
    <androidx.cardview.widget.CardView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        app:cardBackgroundColor="#fff0db"
        app:cardCornerRadius="50dp"
        app:cardElevation="8dp">

        <ImageButton
            android:id="@+id/continueButton"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_arrow_forward"
            android:contentDescription="Continue to Elements"
            android:scaleType="centerInside" />

    </androidx.cardview.widget.CardView>

</LinearLayout>